<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/lightDash"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/redis"
        android:gravity="center"
        android:paddingTop="2dp"
        android:paddingBottom="5dp"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="25sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/marqueeTxt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/redis"
        android:gravity="start"
        android:paddingTop="2dp"
        android:paddingBottom="5dp"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:text="At least 12 people were killed in election-day clashes in Bangladesh Sunday, after a bloody campaign overshadowed by a crackdown on the opposition by Prime Minister Sheikh Hasina, who is expected to win a historic but controversial fourth term."
        android:textColor="@color/white"
        android:textSize="12sp"
        android:textStyle="bold" />


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="5dp"
            android:layout_marginTop="10dp"
            android:gravity="start"
            android:paddingTop="2dp"
            android:paddingBottom="5dp"
            android:text="@string/letest_news"
            android:textColor="@color/redis"
            android:textSize="18sp"
            android:textStyle="bold" />

        <GridView
            android:id="@+id/myGridView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:numColumns="2"
            android:stretchMode="columnWidth"
            android:scrollbars="horizontal"
            android:verticalSpacing="8dp"
            android:elevation="5dp"
            />

    </LinearLayout>

</LinearLayout>